@* <html>
    <head>
        <title></title>
    </head>
    <body>
       <h3> @ViewData["PageTitle"] </h3>
    @{
        //to writw c# code

        var employee = ViewData["Employee"] as WebApplication1.Models.Employee;
    }
   <div>

       Name: @employee.Name
   </div>
   <div>
       Email: @employee.Email
   </div>
   <div>
       Department: @employee.Department
   </div>
    </body>
</html> *@


@* <html>
    <head>
        <title></title>
    </head>
    <body>
       <h3> @ViewBag.PageTitle </h3>
   <div>

       Name: @ViewBag.Employee.Name
   </div>
   <div>
       Email: @ViewBag.Employee.Email
   </div>
   <div>
       Department: @ViewBag.Employee.Department
   </div>
    </body>
</html> *@

@model WebApplication1.ViewModels.HomeDetailsViewModel
@{

    ViewBag.Title = "Employee Details";
    var photoPath = "~/Images/" + (Model.Employee.PhotoPath ?? "employee.png");
}

<div class="row justify-content-center m-3">
    <div class="col-sm-8">
        <div class="card">
            <div class="card-header">
                <h1>@Model.Employee.Name</h1>
            </div>

            <div class="card-body text-center">
                <img class="img-thumbnail" src="@photoPath" asp-append-version="true" height="500" width="500" />

                <h4>Employee ID : @Model.Employee.Id</h4>
                <h4>Email : @Model.Employee.Email</h4>
                <h4>Department : @Model.Employee.Department</h4>

            </div>
            <div class="card-footer text-center">
                <a asp-controller="home" asp-action="index" class="btn btn-primary">Back</a>
                <a href="#" class="btn btn-primary">Edit</a>
                <a href="#" class="btn btn-danger">Delete</a>
            </div>
        </div>
    </div>
</div>

    @section Scripts{
    <script src="c:\users\theja kumar\source\repos\webapplication1\webapplication1\js\customscript.js"></script>
    }
